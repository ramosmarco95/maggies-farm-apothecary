name: Maggie's Farm CI/CD Pipeline

on:
  push:
    branches: 
      - main 

permissions:
  contents: write
  pages: write
  id-token: write
  
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    environment: 
      name: github-pages
      url: ${{ steps.deploy-pages.outputs.page_url }}
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install backend dependencies
      working-directory: backend
      run: npm install

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm install

    - name: Build and test backend
      working-directory: backend
      run: |
        npm run build || echo "No build script for backend, skipping..."
        npm run test || echo "No tests yet, skipping..."

    - name: Build and test frontend
      working-directory: frontend
      run: |
        npm run build
        npm run test || echo "No frontend tests yet, skipping..."

    - name: Set up Docker
      uses: docker/setup-buildx-action@v3

    - name: Build Docker images
      run: |
        docker compose -f docker-compose.yml build

    - name: Upload artifact for GitHub Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: frontend/dist

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build


    # Optional: Push images to DockerHub (later when ready)
    # - name: Login to DockerHub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # - name: Push images
    #   run: |
    #     docker push yourdockerhubusername/frontend
    #     docker push yourdockerhubusername/backend
